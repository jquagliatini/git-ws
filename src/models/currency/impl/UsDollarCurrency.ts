import Currency from '../Currency';

// tslint:disable-next-line variable-name
const UsDollarCurrency = {
  symbol: 'USD',
  utf8_symbol: '$',
  cents_per_unit: 100,
};

export default UsDollarCurrency;
